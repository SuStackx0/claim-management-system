# Base URL of the FastAPI backend. Leave empty in dev (the Vite proxy in
# vite.config.ts forwards /claims, /members, /eval, /health to localhost:8000).
# In production (e.g. Render), set this to the deployed API origin.
VITE_API_BASE_URL=
